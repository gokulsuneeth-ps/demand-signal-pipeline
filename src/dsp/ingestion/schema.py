"""Pandera schemas for the raw -> bronze validation step.

Day 2 fills this in: schema for M5's sales/calendar/prices tables, plus the
long-format bronze table this module reshapes them into. Left as a stub for
day 1 so the package structure and CI (which imports src/dsp) are real from
the first commit, rather than added later as an afterthought.
"""

# TODO(day-2): define BronzeSalesSchema with pandera.DataFrameSchema
