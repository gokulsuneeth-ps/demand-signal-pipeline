"""Calendar/event features (day-of-week, holiday flags, SNAP flags).

Stub for day 1. Each function added here on day 3 should be a pure function
(DataFrame in, DataFrame or Series out) so it's trivially unit-testable
without spinning up the full pipeline.
"""
