"""Seasonal-naive and ETS baselines.

Stub for day 1; implemented day 4. These are not throwaway comparisons —
PROBLEM_STATEMENT.md defines "done" as beating seasonal-naive on every
backtest fold, so this module's output is the bar the real model has to
clear, and it ships in the same CI-tested path as the LightGBM model.
"""
