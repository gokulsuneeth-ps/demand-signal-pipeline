"""Rolling-origin cross-validation harness.

Stub for day 1; implemented day 4. Deliberately never a random train/test
split — that leaks future information into training for time series and
silently inflates reported accuracy.
"""
